To run this file, fork or colne the repository
Kindly install venv (for your machine's sake). Be sure to activate it as well
Run pip install -r requirements.txt to download dependencies
Have fun with it
